# Relativa a propósito: el navegador siempre habla con su propio origen. En desarrollo el
# proxy de Vite reenvía /api al backend (ver vite.config.ts) y en producción es el propio
# backend quien sirve este front, así que la misma ruta vale en los dos casos.
VITE_API_URL=/api
